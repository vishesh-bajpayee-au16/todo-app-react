// creating an obj and assigning name to all the paths so that routes can be accessed with calling the path obj 

const PATHS = {
    home: "/",
    profile: "/",
    login: "/login",
    signup: "/signup",
    logout: "/logout"
}

export default PATHS